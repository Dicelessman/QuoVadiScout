<!-- 30ef167e-562b-4d99-b1a6-b54d917ea40c 62f1c917-2fcc-4efb-ae0a-33fc0573db9c -->
# Piano di Implementazione: Sistema Avanzato QuoVadiScout

## Architettura e Priorità

L'implementazione seguirà un approccio a strati: fondamenta tecniche prima, poi funzionalità user-facing. Questo garantisce stabilità e facilita testing incrementale.

## Fase 1: Fondamenta e Infrastruttura (Priorità Critica)

### 1.1 Estensione Modello Dati Firestore

**File da modificare:**

- `script.js` (linee 1010-1038: funzione aggiungiStruttura)
- Firestore collection "strutture"
- Nuova collection "user_notes"
- Nuova collection "activity_log"
- Nuova collection "structure_versions"

**Nuovi campi per strutture:**

```javascript
{
  // Campi esistenti...
  // Nuovi campi:
  stato: 'attiva' | 'temporaneamente_non_attiva' | 'non_piu_attiva',
  indirizzo: '',
  coordinate: { lat: null, lng: null },
  rating: { total: 0, count: 0, average: 0 },
  segnalazioni: [],
  immagini: [],
  lastModified: timestamp,
  lastModifiedBy: userId,
  createdAt: timestamp,
  createdBy: userId,
  version: 1
}
```

**Note personali (collection separata):**

```javascript
{
  userId: string,
  strutturaId: string,
  nota: string,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 1.2 Service Worker per PWA e Offline

**Nuovo file:** `service-worker.js`

Implementazione completa con:

- Cache statica (HTML, CSS, JS)
- Cache dinamica (dati API)
- IndexedDB per persistenza avanzata
- Sincronizzazione background
- Strategia cache-first con fallback network

**File da modificare:**

- `index.html`: aggiungere registrazione service worker e manifest
- Nuovo file: `manifest.json` per PWA

### 1.3 Sistema di Versioning e Activity Log

**Implementazione in:** `script.js`

Nuove funzioni:

- `salvaVersione(struttura, userId)` - salva snapshot prima di ogni modifica
- `getVersionHistory(strutturaId)` - recupera cronologia
- `ripristinaVersione(strutturaId, version)` - rollback
- `logActivity(action, entity, userId)` - log di sicurezza

## Fase 2: Gestione Stati e Note (Features Core)

### 2.1 Stati Strutture

**File da modificare:**

- `script.js` (funzione renderStrutture, linee 121-237)
- `styles.css` (nuovi stili per badge stati)

Aggiungere:

- Select per stato in form struttura
- Badge visivi colorati nelle card
- Filtro per stato nell'header
- Indicatore stato nell'elenco personale

### 2.2 Note Personali

**File da modificare:**

- `script.js` (funzione mostraGestioneElencoPersonale, linee 1629-2015)

Implementare:

- Textarea per note nel modale elenco personale
- Salvataggio automatico su Firestore (collection user_notes)
- Sincronizzazione tra dispositivi
- Indicatore visivo presenza note

## Fase 3: Sistema Filtri Avanzati

### 3.1 Filtri Multi-Criterio

**File da modificare:**

- `script.js` (funzione filtra, linee 645-757)
- `script.js` (funzione mostraRicercaAvanzata, linee 270-580)
- `index.html` (header controls)

Nuovi filtri:

- Range di rating (slider)
- Filtro per stato
- Province multiple (multi-select)
- Data ultimo controllo (date picker)
- Presenza immagini (checkbox)
- Distanza da coordinate (con geolocalizzazione)

### 3.2 Filtri Salvati

Nuovo pulsante "Salva Filtri" che memorizza:

- Combinazione attiva in Firestore (collection user_filters)
- Nome personalizzato
- Dropdown per applicare filtri salvati rapidamente

## Fase 4: Sistema di Esportazione Avanzato

### 4.1 Esportazione Excel

**Nuova libreria:** SheetJS (xlsx.js) via CDN

**File da modificare:** `script.js`

Nuova funzione `esportaExcel(strutture, options)`:

- Layout personalizzabili (completo, compatto, solo contatti)
- Fogli multipli per categorie
- Formattazione celle (colori, bordi)
- Auto-fit colonne

### 4.2 Esportazione PDF

**Nuova libreria:** jsPDF via CDN

**File da modificare:** `script.js`

Nuova funzione `esportaPDF(strutture, options)`:

- Template professionali
- Logo e intestazione personalizzabile
- Tabelle formattate
- Orientamento landscape/portrait
- Esportazione singola struttura o lista

### 4.3 UI Esportazione

Nuovo modale "Opzioni Esportazione" con:

- Radio button: Excel / PDF
- Checkbox: Includi immagini, Include note, Solo elenco personale
- Select: Layout (completo/compatto/custom)
- Preview esportazione

## Fase 5: Sistema Notifiche Push

### 5.1 Web Push API Setup

**Nuovo file:** `push-notifications.js`

Implementare:

- Richiesta permesso browser
- Registrazione subscription Firestore
- Token FCM (Firebase Cloud Messaging)
- Gestione permessi negati

### 5.2 Notifiche Strutture

**File da modificare:** `script.js` (funzione aggiungiStruttura)

Trigger notifiche per:

- Nuova struttura aggiunta
- Struttura modificata (se nell'elenco personale)
- Nuova segnalazione su struttura nell'elenco

**Implementazione Backend:**

Utilizzare Firebase Cloud Functions per inviare notifiche quando:

```javascript
// Cloud Function
exports.notifyNewStructure = functions.firestore
  .document('strutture/{strutturaId}')
  .onCreate(async (snap, context) => {
    // Invia notifica a tutti gli utenti abilitati
  });
```

### 5.3 Feed Attività

**Nuovo modale:** "Feed Attività"

Visualizza:

- Ultime 50 attività dal log
- Filtri: Tipo azione, Periodo temporale, Utente
- Formato timeline con icone
- Link diretti alle strutture

## Fase 6: Mappe Geografiche

### 6.1 Integrazione Leaflet + OpenStreetMap

**Nuova libreria:** Leaflet.js via CDN

**Nuovo file:** `maps.js`

Funzionalità:

- Mappa interattiva nella dashboard
- Marker per ogni struttura con coordinate
- Popup con info essenziali
- Cluster markers per gruppi vicini
- Layer control (Casa/Terreno)

### 6.2 Geocoding

**API:** Nominatim (gratuita OpenStreetMap)

Funzione `geocodifica(struttura)`:

1. Cerca per indirizzo completo
2. Fallback su "Luogo, Provincia"
3. Fallback su solo "Provincia"
4. Salva coordinate su Firestore

### 6.3 Geolocalizzazione Utente

Aggiungere pulsante "Trova Vicino a Me":

- Richiede permesso geolocalizzazione
- Calcola distanza da ogni struttura
- Ordina per distanza crescente
- Mostra distanza nelle card

## Fase 7: Sistema Rating e Segnalazioni

### 7.1 Rating Strutture

**File da modificare:**

- `script.js` (funzione mostraSchedaCompleta)
- `styles.css` (stili stelle rating)

Implementare:

- Widget stelle (1-5) in scheda completa
- Calcolo media rating
- Visualizzazione rating in card compatta
- Storico valutazioni utente

### 7.2 Sistema Segnalazioni

Nuovo pulsante "Segnala Problema" in scheda completa:

- Form categorizzato (dati obsoleti, struttura chiusa, info errate)
- Campo descrizione
- Salvataggio in Firestore (array segnalazioni)
- Notifica admin/referente
- Counter segnalazioni visibile

## Fase 8: Ottimizzazioni Performance

### 8.1 Lazy Loading Immagini

**Implementazione:** Intersection Observer API

```javascript
// Nuovo in script.js
function setupLazyLoading() {
  const imageObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const img = entry.target;
        img.src = img.dataset.src;
        imageObserver.unobserve(img);
      }
    });
  });
  
  document.querySelectorAll('img[data-src]').forEach(img => {
    imageObserver.observe(img);
  });
}
```

### 8.2 Cache Intelligente

Implementare strategia multi-layer:

- Memory cache (variabili JS) - 5 minuti
- localStorage cache - 1 ora
- Service Worker cache - indefinito
- Firestore come source of truth

### 8.3 Paginazione Ottimizzata

**File da modificare:** `script.js` (già presente paginazione base)

Miglioramenti:

- Virtual scrolling per liste lunghe
- Infinite scroll opzionale
- Prefetch pagina successiva
- Skeleton screens durante caricamento

## Fase 9: Personalizzazione UI

### 9.1 Modalità Oscura

**Nuovo file:** `themes.js`

**File da modificare:** `styles.css`

Implementare:

- CSS variables per colori
- Toggle modalità chiara/scura
- Salvataggio preferenza localStorage
- Rispetto preferenza sistema (prefers-color-scheme)
- Transizioni fluide tra temi

### 9.2 Layout Personalizzabile

Implementare drag & drop con libreria SortableJS:

- Riordino colonne dashboard
- Personalizzazione card (campi visibili)
- Layout salvati per utente su Firestore
- Preset default + personalizzati

### 9.3 Preferenze Avanzate

Nuovo modale "Impostazioni":

- Elementi per pagina
- Modalità visualizzazione default
- Notifiche (on/off per tipo)
- Lingua interfaccia
- Privacy e sicurezza
- Reset a default

## Fase 10: Sincronizzazione e Offline

### 10.1 IndexedDB Setup

**Nuovo file:** `db.js`

Schema IndexedDB:

- Store: structures (mirror Firestore)
- Store: pendingChanges (modifiche offline)
- Store: userNotes
- Store: filters
- Indici per ricerca veloce

### 10.2 Sync Manager

Implementare pattern di sincronizzazione:

```javascript
class SyncManager {
  async syncToServer() {
    // Invia modifiche pending a Firestore
    // Risolvi conflitti (last-write-wins)
    // Aggiorna IndexedDB con dati server
  }
  
  async syncFromServer() {
    // Scarica aggiornamenti da Firestore
    // Merge con dati locali
    // Notifica utente se conflitti
  }
  
  async handleConflict(local, remote) {
    // Mostra dialog risoluzione conflitti
    // Opzioni: Mantieni locale, Usa remoto, Merge manuale
  }
}
```

### 10.3 Indicatori UI

Aggiungere:

- Badge "Offline" nella topbar quando disconnesso
- Counter modifiche pending
- Progress bar sincronizzazione
- Toast notifications per sync completato

## File da Creare

1. `service-worker.js` - PWA e offline
2. `push-notifications.js` - Web Push API
3. `maps.js` - Leaflet integration
4. `db.js` - IndexedDB wrapper
5. `sync-manager.js` - Sync logic
6. `themes.js` - Tema chiaro/scuro
7. `export.js` - Excel e PDF export
8. `manifest.json` - PWA manifest
9. `firebase-functions/` - Cloud Functions per notifiche

## File da Modificare

1. `script.js` - Maggior parte logica business
2. `styles.css` - Nuovi stili per tutte le feature
3. `index.html` - Nuovi controlli UI
4. `dashboard.html` - Integrazione mappa
5. `dashboard.js` - Stats e mappa

## Librerie da Aggiungere (via CDN)

```html
<!-- In index.html head -->
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
```

## Testing e Rollout

Dopo ogni fase:

1. Test funzionale della feature
2. Test compatibilità browser (Chrome, Firefox, Safari, Edge)
3. Test mobile (iOS Safari, Android Chrome)
4. Test offline (se applicabile)
5. Deploy su Firebase Hosting
6. Monitoraggio errori prime 24h

## Note Implementative

- Utilizzare Firestore composite indexes per query complesse
- Implementare rate limiting per API calls
- Validazione input sia client che server-side
- Error boundaries per graceful degradation
- Analytics per monitorare uso features
- Documentazione inline per manutenibilità

## Stima Temporale

- Fase 1-2: ~3-4 ore (fondamenta)
- Fase 3-4: ~2-3 ore (filtri ed export)
- Fase 5-6: ~3-4 ore (notifiche e mappe)
- Fase 7-8: ~2-3 ore (rating e performance)
- Fase 9-10: ~3-4 ore (UI e offline)

**Totale stimato: 13-18 ore di sviluppo**

### To-dos

- [ ] Estendere modello dati Firestore con nuovi campi (stato, coordinate, rating, etc.)
- [ ] Creare service-worker.js per PWA e funzionalità offline
- [ ] Implementare sistema versioning e activity log
- [ ] Aggiungere gestione stati strutture (attiva/non attiva/chiusa)
- [ ] Implementare note personali per strutture in elenco
- [ ] Estendere sistema filtri con multi-criterio e combinazioni
- [ ] Implementare salvataggio e riutilizzo combinazioni filtri
- [ ] Implementare esportazione Excel con layout personalizzabili
- [ ] Implementare esportazione PDF con template professionali
- [ ] Configurare Web Push API e Firebase Cloud Messaging
- [ ] Implementare notifiche per nuove strutture e modifiche
- [ ] Creare feed attività con log modifiche
- [ ] Integrare Leaflet e OpenStreetMap per visualizzazione mappa
- [ ] Implementare geocoding automatico per indirizzi
- [ ] Aggiungere geolocalizzazione utente e ricerca per distanza
- [ ] Implementare sistema rating a stelle per strutture
- [ ] Creare sistema segnalazioni problemi/dati obsoleti
- [ ] Implementare lazy loading per immagini
- [ ] Ottimizzare sistema cache multi-layer
- [ ] Implementare modalità oscura con CSS variables
- [ ] Aggiungere personalizzazione layout con drag & drop
- [ ] Creare pannello impostazioni avanzate utente
- [ ] Configurare IndexedDB per persistenza offline
- [ ] Implementare SyncManager per sincronizzazione bidirezionale
- [ ] Aggiungere indicatori UI per stato offline e sync